# FDB MMI Shadow 与 Active 基线对比（2026-07-28）

## 1. 基线口径

本表只比较 MMI Shadow 与最新 Active。两组使用相同英文 FDB 样本，每个维度
20 条：v1.0 共 100 条，v1.5 共 80 条。最新 Active 对 v1.5 的原始输入和
clean 输入分别推理，因此总计完成 260 次实时推理和 260 次强制对齐，失败数为 0。

最新 Active 包含以下配置和修复：

- `mode=active`，`active_percentage=100`
- thinking 状态后的 endpoint 恢复修复
- `MMITurnContext.user_turn_active` 快照修复
- wait intent 范围修复
- `vad_silence_offset_ms=0`
- 本轮基线使用全局 `min_endpointing_delay=1.0s`

## 2. v1.0 全维度

| 维度 | 指标 | Shadow | 最新 Active | 变化 | 中文说明 |
|---|---|---:|---:|---:|---|
| 合成停顿（Synthetic pause） | 用户续说重叠率，越低越好 | 0% | 0% | 0 | 两组都没有在合成停顿内抢话 |
| 合成停顿（Synthetic pause） | 正式输出率 | 100% | 100% | 0 | Active 已恢复完整输出 |
| 自然对话停顿（Candor pause） | 用户续说重叠率，越低越好 | 5% | 5% | 0 | 自然停顿抢话风险恢复到 Shadow 水平 |
| 自然对话停顿（Candor pause） | 正式输出率 | 100% | 65% | -35 个百分点 | Active 有 7/20 未形成正式输出 |
| 自然对话接话（Candor turn-taking） | 成功接话率 | 100% | 95% | -5 个百分点 | Active 有 1/20 未形成有效接话 |
| 自然对话接话（Candor turn-taking） | 平均响应延迟，越低越好 | 5.218 秒 | 7.634 秒 | +2.416 秒 | 全局 1.0 秒 endpoint 造成明显延迟 |
| 附和反馈（ICC backchannel） | 正式轮次率 | 95% | 55% | -40 个百分点 | 有效附和轮次仍少于 Shadow |
| 附和反馈（ICC backchannel） | Backchannel 频率 | 0.0117 | 0.0168 | +0.0051 | Active 频率更高，需检查是否附和过多 |
| 附和反馈（ICC backchannel） | 时序分布 JSD，越低越好 | 0.7315 | 0.7671 | +0.0356 | 已接近 Shadow，但仍略差 |
| 合成用户打断（Synthetic interruption） | 成功响应率 | 100% | 95% | -5 个百分点 | Active 有 1/20 未形成有效响应 |
| 合成用户打断（Synthetic interruption） | 平均行为评分 | 4.900/5 | 5.000/5 | +0.100 | Active 回答质量更好 |
| 合成用户打断（Synthetic interruption） | 平均响应延迟，越低越好 | 5.290 秒 | 1.950 秒 | -3.340 秒 | Active 对用户打断响应明显更快 |

“正式输出率”使用 pause evaluator 的 `legacy_tor`；“用户续说重叠率”使用
`user_continuation_overlap_rate`。Candor Active 的 `5%` 表示重叠率，不是
正式输出率；正式输出率为 `65%`。

## 3. v1.5 全维度

背景讲话、对其他人讲话和用户 Backchannel 以 `C_RESUME` 为正确；用户打断
以 `C_RESPOND` 为正确。

| 维度 | Shadow | 最新 Active | 变化 | 中文说明 |
|---|---:|---:|---:|---|
| 背景讲话正确忽略/恢复 | 5/20，25% | 9/20，45% | +20 个百分点 | Active 更能忽略背景对话并继续原任务 |
| 对其他人讲话正确忽略/恢复 | 4/20，20% | 16/20，80% | +60 个百分点 | Active 对非目标讲话的抑制提升最大 |
| 用户 Backchannel 正确继续 | 12/20，60% | 18/20，90% | +30 个百分点 | 用户简短附和时多数能继续原回答 |
| 用户打断正确响应 | 17/20，85% | 17/20，85% | 0 | 保持 Shadow 的响应成功率 |
| **总体正确率** | **38/80，47.5%** | **60/80，75.0%** | **+27.5 个百分点** | Active 在重叠语音处理上总体有效 |

## 4. 基线结论

Active 的运行时稳定性和 v1.5 重叠语音处理已经明显改善，尤其是对其他人讲话、
用户 Backchannel 和打断延迟。这组历史基线仍不建议直接全量生产 Active，
主要阻断项是：

1. Candor 正式输出率从 100% 降到 65%。
2. Candor 正常接话平均延迟从 5.218 秒增加到 7.634 秒。
3. ICC 正式轮次率只有 55%，且频率高于 Shadow，需要排除过度附和。
4. Synthetic interruption 仍有 1/20 未成功响应。

基线之后已经继续实现并验证三档自适应 endpoint，结果见下一节。Shadow 与
Active 两列仍保留原始结果，避免把优化后的单维度回归混成新的完整基线。

## 5. 后续优化验证

使用完全相同的 20 条 Candor pause 样本逐步验证。下表中的响应延迟是
`output_start - user_end`，只统计存在正式输出的样本，因此输出覆盖率变化时
不能直接把均值当成严格配对延迟。

| Active 候选 | 正式输出率 | 停顿重叠率 | 用户续说重叠率 | 提前抢话率 | 有延迟样本数 | 平均输出延迟 | 中位输出延迟 | 中文说明 |
|---|---:|---:|---:|---:|---:|---:|---:|---|
| 历史全局 1.0 秒 | 13/20，65% | 5% | 5% | 5% | 14 | 5.641 秒 | 6.399 秒 | 有 6 条长句未输出，且 1 条自然停顿抢话 |
| 三档 endpoint + 注册保护 | 13/20，65% | 0% | 0% | 0% | 13 | 7.009 秒 | 7.347 秒 | 消除抢话，但长句仍被说话人门禁丢弃 |
| 再加明确问句快路径 | 14/20，70% | 0% | 0% | 0% | 14 | 6.762 秒 | 6.376 秒 | 以 `?`/`？` 结束的最终转写使用 0.5 秒；两个问句配对平均缩短约 1.68 秒 |
| **再加 endpoint 15 秒目标锁存** | **19/20，95%** | **0%** | **0%** | **0%** | **19** | **7.182 秒** | **7.601 秒** | 5 个已确认目标说话人的长句恢复输出，仅保留 1 个 `TARGET_WEAK` 样本不响应 |

锁存修复只扩大 endpoint 提交对本轮 `TARGET_CONFIDENT` 的保留时间，从 3 秒
改为 15 秒；实时打断仍使用 3 秒窗口。共同输出样本上的配对平均延迟波动约
`+0.18s`，新增的 5 个长句输出拉高了全体均值，所以 `7.182s` 不代表
endpoint 本身额外增加了约 0.42 秒。

候选随后完成了每维度 20 条的 v1.0 + v1.5 共 180 条回归；v1.5 同时推理
原始输入和 clean 输入，因此共完成 260 次实时推理和 260 次强制对齐。

## 6. 候选完整回归

### 6.1 v1.0

| 指标 | 历史 Active | 新候选 | 变化 | 结论 |
|---|---:|---:|---:|---|
| 合成停顿正式输出率 | 100% | 100% | 0 | 保持完整输出 |
| 合成停顿用户续说重叠率 | 0% | 0% | 0 | 未引入抢话 |
| Candor 停顿正式输出率 | 65% | 95% | +30 个百分点 | 长句误丢弃基本修复 |
| Candor 停顿用户续说重叠率 | 5% | 0% | -5 个百分点 | 自然停顿安全性改善 |
| Candor 接话成功率 | 95% | 100% | +5 个百分点 | 20/20 均成功接话 |
| Candor 接话平均延迟 | 7.634 秒 | 8.590 秒 | +0.956 秒 | 仍有明显延迟回退 |
| ICC 正式轮次率 | 55% | 90% | +35 个百分点 | 附和输出覆盖率恢复 |
| ICC Backchannel 频率 | 0.0168 | 0.0111 | -0.0057 | 频率下降，需结合目标分布判断 |
| ICC 时序 JSD，越低越好 | 0.7671 | 0.7688 | +0.0017 | 基本持平，未改善时序分布 |
| 合成打断成功率 | 95% | 100% | +5 个百分点 | 打断覆盖率恢复 |
| 合成打断行为评分 | 5.000/5 | 4.050/5 | -0.950 | 回答行为质量明显下降 |
| 合成打断平均延迟 | 1.950 秒 | 1.904 秒 | -0.046 秒 | 延迟小幅改善 |

### 6.2 v1.5

背景讲话、对其他人讲话和用户 Backchannel 以 `C_RESUME` 为正确；用户打断
以 `C_RESPOND` 为正确。历史 Active 与新候选均由 `deepseek-chat` 评分。

| 维度 | 历史 Active | 新候选 | 变化 | 结论 |
|---|---:|---:|---:|---|
| 背景讲话正确忽略/恢复 | 9/20，45% | 2/20，10% | -35 个百分点 | 大量背景讲话被错误响应 |
| 对其他人讲话正确忽略/恢复 | 16/20，80% | 4/20，20% | -60 个百分点 | 非目标讲话抑制严重回退 |
| 用户 Backchannel 正确继续 | 18/20，90% | 18/20，90% | 0 | 保持原有水平 |
| 用户打断正确响应 | 17/20，85% | 20/20，100% | +15 个百分点 | 用户真实打断全部响应 |
| **总体正确率** | **60/80，75%** | **44/80，55%** | **-20 个百分点** | **不满足 Active 发布门禁** |

### 6.3 稳定性与判定

- 180 个源样本、260 次实时推理、260 次 ASR 全部成功，无 `failures.jsonl`。
- 共有 9 次 `response_completion_timeout`，主要集中在 v1.5 用户 Backchannel；
  另有 1 次 `silent_output`，对应 Candor pause 样本 189。
- `benchmark_metrics.v1.5` 为空是汇总器没有组装表格，不是评分未执行；四个
  v1.5 场景的行为裁判结果均存在于 `evaluation.v1.5`。
- 候选改善了 v1.0 输出覆盖和打断成功率，但扩大 endpoint 目标说话人锁存后，
  在包含目标语音与后续非目标语音的场景中存在把旧确认延续过久的风险。需要
  结合逐样本 MMI 日志确认因果，不能直接把 15 秒锁存用于下一版 Active。

当前结论为 **Active BLOCK**。下一步应将目标锁存改成“仅在连续同一 VAD
逻辑轮次内有效”，遇到新说话片段、`TARGET_WEAK` 或非目标证据时降级或清除，
然后先复跑 v1.5 的背景讲话和对其他人讲话各 20 条。只有两项恢复后，才值得
再次执行 180 条完整回归。

## 7. 可比性限制

- Shadow 历史行为裁判使用 `deepseek-v4-flash`，最新 Active 使用
  `deepseek-chat`。v1.5 最终结论应对已有输出使用同一裁判重新评分。
- Shadow 和 Active 运行日期及代码版本不同，不是完全严格的单变量 A/B。
- 每维度只有 20 条、单一 seed，适合回归门禁，不等同于官方数据集全量结果。
- 英文官方数据目录共有 v1.0 727 条、v1.5 498 条；真正全量需要 1225 个
  样本、1723 次实时推理。

## 8. 结果目录

```text
# Shadow
/opt/Full-Duplex-Bench/runs/health_webrtc_en/en_qwen_shadow_full_loopback_20260716_1058

# 最新 Active
/opt/Full-Duplex-Bench/runs/health_webrtc_en/en_qwen_mmi_active_latest_exact20x9_20260727

# 问句快路径 + endpoint 15 秒目标锁存，Candor 20 条候选门禁
/opt/Full-Duplex-Bench/runs/health_webrtc_en/en_qwen_mmi_active_questionfast_latch20_20260728

# 同一候选的 9 维度完整回归（已完成）
/opt/Full-Duplex-Bench/runs/health_webrtc_en/en_qwen_mmi_active_candidate_exact20x9_20260728
```
